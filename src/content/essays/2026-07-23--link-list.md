---
title: "Creating a list of links with Astro collections"
slug: "link-list-astro"
draft: True
publishedDate: 2026-07-23
introduction: |
excerpt: "Example excerpt"
thumbnail: "/images/example.jpg"
thumbnailCaption: "Example Caption"
categories: ['Development', 'Astro']
highlight: False
---

One of my favorite things abous Astro(AddLink) 
is that Collections(AddLink) can be used for a variety of things
beyond *mere blogging.*

So today I wanted to explore this,
inspired by a question I've been wondering for a long time:

**How can I share more of myself with whoever's out there listening?**

At the same time,
how can this effort not go to waste
in platforms uncaring for anything other than
increasing shareholder value?

So I thought, 
"why not links I found interesting on my website?!"

***Genius.***

## 1. Create a new Content Collection in Astro

First of all,
open `content.config.js`

Import `defineCollection` and `z` from `astro:content`, 
and `glob` from `astro/loaders:
```
import { defineCollection, z } from "astro:content";
import { glob } from 'astro/loaders';
```

Next,
inside `defineCollection`,
define Links Collection:
```
const links = defineCollection({
    loader: glob({ 
        pattern: "**/*.md", // TODO: Describe what this line does in a comment 
        base: "./src/content/links" // The Path to your Content Collection folder
    }), 
    schema: z.object({
        // List of Content Collection Fields
        url: z.string().url(), // Validates String is an URL
        status: z.enum([
            "inbox",
            "published",
            "archived",
        ]).default("inbox")
    })
})
```

Export 
your Collection below:
```
export const collections = {
    links
}
```

## 2. Create (file? I don't know how to title this step)

Second!
Inside `src/content/`
create a new folder called
`links`

Create a new file inside `src/content/links`

Inside this file, type:
```
---
# Link v.1
url: https://www.youtube.com/watch?v=oZame1Brs9k
status: 'inbox' # 'inbox' | 'published' | 'archived'
---
```

## 3. Display Links Content Collection in Astro's Front End

Inside `src/pages/index.astro`
import and load Links Content Collection:
```
import { getCollection } from 'astro:content';

const allLinks = await getCollection('links')
const links = await getCollection(
  "links",
  ({ data }) => data.status === "published" // Filter Callback
);
```