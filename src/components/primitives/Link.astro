---
const {
  href = "#",
  class: className = "",
  external = false,
  ...rest
} = Astro.props;
---

<a
  href={href}
  class={className}
  target={external ? "_blank" : undefined}
  rel={external ? "noopener noreferrer" : undefined}
  {...rest}
><slot /></a>