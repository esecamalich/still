---
import styles from './Hamburger.module.css'

const {
  class: className = "",
  ...rest
}= Astro.props
---
<button class={`hamburger ${styles['hamburger-wrapper']} ${className}`} {...rest}>
  <span class={styles['line']}></span>
  <span class={styles['line']}></span>
</button>